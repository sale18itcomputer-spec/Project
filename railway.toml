[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "npx ts-node --transpile-only src/mcp.ts"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
