# ─────────────────────────────────────────────────────────────────────────────
# Production-only — auto-loaded by Next.js on `next build` / `next start`
# NOT loaded during `next dev`. Safe to commit.
# ─────────────────────────────────────────────────────────────────────────────

# Production domain
NEXT_PUBLIC_SITE_URL=https://project.limperialtech.com

# NEXT_PUBLIC_DEV_BYPASS_LOCK is deliberately absent here.
# The passcode lock is fully active in production.
